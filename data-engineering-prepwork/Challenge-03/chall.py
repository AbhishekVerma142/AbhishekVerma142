from typing import List

def find_max_in_list(numbers: List[int]) -> int:
    """
    Your challenge is to write a function that takes in a list of integers and returns the highest number in the list.
    If the list is empty, return None.
    """
    pass
