import sqlite3

def create_table(db_path=':memory:'):
    """
    Your challenge is to create a SQLite database table with the following schema:
    Table Name: employees
    Columns:
    - id (INTEGER, primary key)
    - name (TEXT)
    - age (INTEGER)
    - salary (REAL)

    The function should connect to a new or existing SQLite database at the db_path, create the table, and return None.
    """
    pass
