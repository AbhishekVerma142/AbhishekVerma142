def is_palindrome(s: str) -> bool:
    """
    Your challenge is to write a function that takes a string as an argument and returns True if the string is a palindrome
    (reads the same forwards as backwards), and False otherwise. The function should ignore case, punctuation, and whitespace.
    """
    pass
