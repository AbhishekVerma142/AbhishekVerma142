from typing import List

class Student:
    """
    Your challenge is to implement a Student class. The Student class should have the following methods:

    - __init__(self, name: str, grades: List[int]): A constructor that initializes the student with a name and a list of grades.

    - add_grade(self, grade: int) -> None: A method that adds a grade to the student's list of grades.

    - average(self) -> float: A method that calculates and returns the average of the student's grades.
    If the student has no grades, it should return 0.0.
    """
    pass
